# API Глоссария по Реактивному Программированию

## 📖 Описание проекта

Данный проект реализует микросервисную архитектуру для управления глоссарием в рамках Выпускной Квалификационной Работы (ВКР). Система состоит из трех компонентов: двух независимых бэкенд-сервисов и одного фронтенд-приложения.

**Бэкенд-сервисы:** Оба сервиса используют единую бизнес-логику (CRUD-операции) и работают с общей базой данных через монтируемый том, но используют разные транспортные технологии:

1. **REST Backend (FastAPI)**: Предоставляет стандартный HTTP API (JSON) для внешнего доступа и клиентских приложений, включая интерактивную документацию.

2. **gRPC Backend (Python)**: Предоставляет высокопроизводительный RPC-интерфейс для взаимодействия с другими внутренними сервисами.

**Фронтенд:** 3. **Frontend (React/TypeScript)**: Одностраничное приложение (SPA), которое взаимодействует исключительно с **REST Backend** для отображения и управления терминами.

## 🛠️ Используемые Технологии

| Категория       | Технология                   | Описание                                                       |
| --------------- | ---------------------------- | -------------------------------------------------------------- |
| **Оркестрация** | **Docker Compose**           | Управление жизненным циклом и совместным запуском контейнеров. |
| **REST API**    | **FastAPI, Uvicorn**         | Высокопроизводительный асинхронный HTTP-фреймворк.             |
| **gRPC**        | **grpcio**                   | Фреймворк для удаленного вызова процедур (RPC) по HTTP/2.      |
| **Фронтенд**    | \*\*React, TypeScript        | Клиентское приложение для визуализации данных.                 |
| **База данных** | **SQLite, SQLModel**         | Легковесная файловая БД и ORM на основе Pydantic и SQLAlchemy. |
| **Язык**        | **Python 3.11+, JavaScript** | Основной язык для бэкендов и фронтенда.                        |

## 🏗️ Архитектура и Структура Проекта

Проект использует структуру монорепозитория, разделенную на два бэкенда, фронтенд и общий модуль `common`.

```
.
├── docker-compose.yaml         # Файл оркестрации для запуска всех сервисов
│
├── common/                     # Общие модули и логика
│   ├── database.py             # Настройка подключения к БД (через ENV)
│   ├── models.py               # Определения моделей SQLModel
│   └── crud.py                 # CRUD-функции для работы с БД
│
├── data/                       # Монтируемый том: содержит файл glossary.db
│   └── glossary.db
│
├── rest_backend/               # REST-микросервис (FastAPI)
│   ├── Dockerfile              # Инструкции для сборки образа REST
│   ├── requirements.txt
│   └── app/
│       └── main.py             # Основное приложение с роутами
│
├── grpc_backend/               # gRPC-микросервис (Python)
│   ├── Dockerfile              # Инструкции для сборки образа gRPC
│   ├── requirements.txt
│   ├── proto/                  # Protobuf-схемы
│   │   └── glossary.proto
│   └── app/
│       ├── server.py           # Запуск gRPC-сервера
│       └── client.py           # Локальный клиент для тестирования gRPC
│
└── frontend/                   # Фронтенд-приложение (React)
├── index.html                  # Главная HTML-страница
    ├── package.json            # Зависимости и скрипты Node.js/Vite
    └── src/                    # Исходный код React/TSX
        ├── main.tsx            # Точка входа (инициализация React)
        ├── App.tsx             # Основной компонент приложения
        ├── types.ts            # Общие TypeScript типы данных
        ├── hooks/
        │   └── useGlossary.ts  # Кастомный хук для логики взаимодействия с REST
        └── components/         # Коллекция UI-компонентов
            ├── Card/           # Компонент для отображения одного термина
            └── MindMap/        # Компонент для визуализации семантического графа
```

## 🚀 Развертывание с использованием Docker Compose

Развертывание обоих микросервисов, использующих общий том данных, осуществляется одной командой.

### 1. Запуск и Сборка Сервисов

Убедитесь, что Docker запущен. Выполните команду в корневой директории проекта:

```
docker-compose up --build -d
```

После выполнения команды будет произведена сборка образов (REST, gRPC) и запуск контейнеров в фоновом режиме.

### 2. Проверка Работоспособности Сервисов

После запуска оба бэкенд-сервиса будут доступны на хост-машине:

| Сервис           | Адрес                   | Описание                        |
| ---------------- | ----------------------- | ------------------------------- |
| **REST Backend** | `http://localhost:8000` | HTTP-доступ к данным глоссария. |
| **gRPC Backend** | `localhost:6000`        | Высокоскоростной RPC-интерфейс. |

#### A. Проверка REST API (HTTP)

Доступ к интерактивной документации FastAPI:

- **Swagger UI:** `http://localhost:8000/docs`

- **Проверка данных:** `curl http://localhost:8000/terms/`

#### B. Проверка gRPC API (RPC)

Для проверки gRPC можно использовать локальный Python-клиент.

1. Убедитесь, что у вас установлены зависимости `grpcio` и `grpcio-tools` в вашем локальном виртуальном окружении.

2. Выполните скрипт:

```
python grpc_backend/app/client.py
```

Либо можно использовать инструмент **grpcurl**, который позволяет вызывать RPC-методы без необходимости писать клиентский код.

1. Проверка списка доступных сервисов

Так как сервер работает **без TLS**, нужно использовать флаг `-plaintext`:

```
grpcurl -plaintext localhost:6000 list
```

2. Просмотр методов конкретного сервиса

```
grpcurl -plaintext localhost:6000 list app.GlossaryService
```

3. Вызов метода `GetTerms`

```
grpcurl -plaintext \
  -d '{"skip":0, "limit":10}' \
  localhost:6000 app.GlossaryService/GetTerms
```

4. Вызов метода GetTermById

```
grpcurl -plaintext \
  -d '{"id": 1}' \
  localhost:6000 app.GlossaryService/GetTermById
```

#### C. Проверка Frontend (Клиентское приложение)

**Внимание:** На данный момент фронтенд-приложение **не контейнеризировано** и запускается локально для упрощения разработки.

1. Убедитесь, что **REST Backend** запущен через Docker Compose и доступен по адресу `http://localhost:8000`.

2. Для запуска клиена выполните следующие шаги в директории `frontend/`:

   1. Установка зависимостей

      ```
      npm install
      ```

   2. Запуск в режиме разработки
      ```
      npm run dev
      ```

Приложение будет доступно по адресу, указанному Vite (обычно `http://localhost:5173`).

## 🗑️ Остановка и Удаление Контейнеров

Чтобы остановить и удалить контейнеры, а также связанные сети (но **сохранить** файл `glossary.db` в папке `./data`):

```
docker-compose down
```
